import XCTest
@testable import SwiftUI_TaiwanMemeCat

final class SwiftUI_TaiwanMemeCatTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_TaiwanMemeCat().text, "Hello, World!")
    }
}
